{
  "name": "slop-content",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "validate": "bun run scripts/validate-frontmatter.ts",
    "validate:fix": "bun run scripts/validate-frontmatter.ts --fix",
    "validate:author": "bun run scripts/validate-author.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "glob": "^11.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
